<Window x:Class="AutoCadGltfExporter.UI.ExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export GLB Options" Height="280" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" x:Name="EnableDracoCheckBox" Content="Enable Draco compression (3D meshes only)" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12"
                    IsEnabled="{Binding IsChecked, ElementName=EnableDracoCheckBox}">
            <TextBlock Text="Compression level (0-10):" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="CompressionLevelTextBox" Width="60" Text="7"/>
        </StackPanel>
        <CheckBox Grid.Row="2" x:Name="IncludePropertiesCheckBox" Content="Include entity handles &amp; XData in extras" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="Export" Width="88" Height="28" Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="88" Height="28" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
